import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import "bootswatch/dist/journal/bootstrap.min.css" //Libreria del estilo(journal rosa)
createApp(App).use(router).mount('#app')
